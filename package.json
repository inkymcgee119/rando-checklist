{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker-build": "docker build -t rando-checklist .",
    "docker-run": "docker run -d -p 80:8080 rando-checklist",
    "tauri": "tauri",
    "tauri-dev": "tauri dev",
    "tauri-build": "tauri build"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.2.0",
    "@tauri-apps/cli": "1.1.0",
    "nuxt": "3.0.0",
    "nuxt-icon": "^0.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "1.1.0",
    "serve": "^14.2.0"
  }
}
