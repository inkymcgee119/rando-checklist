{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build-docker": "docker build -t zeldo-tracker .",
    "tauri": "tauri",
    "electron-start": "electron ."
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.2.0",
    "@tauri-apps/cli": "1.1.0",
    "nuxt": "3.0.0",
    "nuxt-icon": "^0.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "1.1.0"
  }
}
